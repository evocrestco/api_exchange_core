# Database Configuration for PostgreSQL (Docker)
DB_HOST=localhost
DB_PORT=15432
DB_NAME=testdb
DB_USER=testuser
DB_PASSWORD=testpassword
DB_TYPE=postgres

# Development Settings
APP_ENV=development
DB_ECHO=false
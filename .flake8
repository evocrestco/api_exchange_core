[flake8]
max-line-length = 120
extend-ignore = E203, W503
exclude = 
    .git,
    __pycache__,
    .venv,
    venv,
    *.egg-info,
    build,
    dist
per-file-ignores = 
    tests/*:E402
# Enable our custom exception checker
enable-extensions = EXC001
